# 每日课程备课与出版规范

本规范落实用户 2026-09-03 的明确反馈：不能把共同模板换标题当作每日教学。原有生成器已删除。

## 已完成与尚未完成

- 122 天大纲在 app/curriculum.ts，属于计划，不等于 122 天正文已经写完。
- 9 月 1 日保留原始专题课与历史记录。
- 9 月 2—7 日已逐课重写，18 门课程在 app/courseware/YYYY-MM-DD.ts。
- app/courseware/index.ts 仅注册完成备课的日期。未注册日期必须显示“待备课”，不可用通用正文填充，也不允许打卡或作答。
- 不改变日期、任务 ID，不删除/重置用户打卡、笔记、收藏、答题；历史答案保留当时问题与参考，不追溯改分。

## 备课步骤

1. 读用户背景、总纲和当天三个既定主题，写清先修、与前后日的衔接和当天独立产出。
2. 检索并实际阅读相关原论文、官方课程、官方文档或作者开源仓库。每日至少两个相关原始来源；不要只看搜索摘要，不用聚合内容冒充一手证据。
3. 写原创中文讲解，不复制整篇论文或教程。给来源链接及明确章节/函数定位。标明项目事实、教学假设、个人建议和未验证推断；main 链接会变化，应记录验证日期，关键复现优先固定提交。
4. 三课分工：
   - 基础知识：定义、直觉、推导、符号解释、数值算例、适用条件和反例。
   - 代码精读：真实源码入口、调用链、shape/轴/时间语义、原创最小实现、逐段说明、正确与故意失败的测试。教学简化必须明说，不能冒充完整上游实现。
   - 路线洞察：从当天机制推导至少两条选择的具体收益/代价，说明个人算力、RAW/感知经验或 SO-101 项目如何验证；不重复通用“基线—晋级—停止”套话。
5. 每课正文至少四节，每节至少两段解释，正文至少 900 字符。这只是挡住空壳的下限，不是教学充分性的证明。重点难题应写得更详尽；禁止靠重复句子、背景套话或代码凑数。
6. 基础与路线课至少有一处解释清楚的公式/定量模型，给假设、单位和算例。公式用 LaTeX，经 KaTeX 严格解析。不要只罗列公式，不从未经验证的硬件性能推导“实测”结果。
7. 每课提供独立练习、可核对解答、自检题与主题专属关键词。每日综合题必须连接当天内容，不能套通用题干。当前评分只是关键词辅助，不应宣称是语义判分或模型评审。
8. 核心课程总计 90—105 分钟，给复盘留出时间，不超过用户每天 120 分钟上限。延伸阅读明确非当天必读全文。

## 自动备课与发布

- 每次定时运行按 Asia/Shanghai 确定日期，优先补足当天/次日缺口，再逐课编写最早未备日期，目标维持至少未来 4 天的可用正文。
- 不改变已完成日期内容，除非用户明确要求纠错；用户本次授权了 9 月 2/3 日的重写，后续不能泛化为随意改写历史。
- 自动任务不能代替用户完成学习或制造打卡。不能执行任何真实机械臂指令或上传客户数据。
- 不自动改总纲方向。改变主题仍需符合既定 JD/面经证据门槛；给已有主题编写完整正文不属于改变学习方向，不应因此跳过备课。
- 首先检查工作树和最新已发布源码；若有其他线程正在编辑重叠文件，不覆盖、不用旧副本发布将其回滚，报告冲突并保留现有线上版。
- 本地任务需要电脑与应用运行、网络和权限可用。执行失败必须报告，不能用模板或空段落掩盖缺课。

## 验收

- npm run check:curriculum：检查当日/次日覆盖、全部已备课的正文结构、来源定位、公式、代码和题目唯一性、段落重复、同轨道正文相似度。可加 -- --through=YYYY-MM-DD 检查备课储备。
- npm run test:examples：在含 PyTorch 的 Python 环境实际运行全部原创教学例子。可用 COURSE_TEST_PYTHON 指定解释器；未执行时不得报告“通过”。不要自动运行下载到的任意上游脚本。
- npm run test:daily：内存 SQLite 检查日期、午夜/恢复、打卡、未备课拒绝、旧记录保留；不连接线上数据库。
- npx tsc --noEmit --incremental false、npm run lint、npm run build。构建包含课程内容门禁，禁止为了发布绕过或降低阈值。
- 人工/代理逐课复读：公式与文字一致、代码说明与实际行为一致、相邻日解决不同问题、资料真正相关。低相似度不证明正确，更不等于专家精审。
- 审计记录放 content/courseware-audits/YYYY-MM-DD.md，列新增日期、来源、验证结果、未完成范围。使用 Sites 在现有访问范围发布，不新建站点、不改变 D1。
