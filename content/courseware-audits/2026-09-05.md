# 2026-09-05 课程备课与质量验收

## 新增范围

- 按 Asia/Shanghai 当天为 2026-09-05，原有已备正文至 09-08；为维持至少未来四天，新编并注册 09-09 三课。
- 基础课：Autograd 动态图、VJP、叶子/grad_fn、梯度累加与 grad modes。
- 代码课：detach 静默断图、in-place 版本检查、None/全零/非有限梯度与 Engine 源码调度入口。
- 路线课：用参数预算、梯度覆盖和正负对照在云训练前分离数据与梯度故障，并落到 RGB/RAW 融合与 SO-101 离线 batch。
- 三课时间 32+36+27=95 分钟，在 120 分钟日预算内保留 25 分钟复盘。

## 原始来源与实际阅读

- [PyTorch · Automatic Differentiation with torch.autograd](https://docs.pytorch.org/tutorials/beginner/basics/autograd_tutorial.html)：阅读 Tensors, Functions and Computational graph、Computing Gradients 与 Jacobian product，核对动态建图、叶子梯度和累加。
- [PyTorch · Autograd mechanics](https://docs.pytorch.org/docs/main/notes/autograd.html)：阅读 saved tensors、requires_grad、grad modes、in-place correctness checks，核对 `eval()` 不会关闭梯度。
- [PyTorch · engine.cpp](https://github.com/pytorch/pytorch/blob/main/torch/csrc/autograd/engine.cpp)：定位 `Engine::execute`、`compute_dependencies`、`evaluate_function` 和 `ReadyQueue`，说明 main 链接会变且核对日期为 2026-09-05。
- [PyTorch · torch.autograd.Function source](https://github.com/pytorch/pytorch/blob/main/torch/autograd/function.py)：定位 `Function`、`save_for_backward` 与 `once_differentiable`；课内代码为原创最小测试，不冒充引擎实现。

## 内容复读

- 公式与算例：手算 `L=x²+3x` 在 x=2 的分支梯度为 7，与代码一致；VJP 和参数组覆盖率均解释符号、限制和误用边界。
- 代码行为：detach 负例通过 `0*w` 保留零导数可达路径，用于区分全零与 None；in-place 负例在 backward 所需中间值被修改后必须触发版本错误。
- 简化范围：示例仅使用 CPU 标量/小张量，不包含分布式、混合精度或真实 VLA 架构；课文明确这是启动门禁，不是性能或真机效果证明。
- 前后衔接：09-08 先固定张量契约，09-09 验证学习信号，09-10 再进入 optimizer / learning-rate / warmup；不与前日的 shape 检查重复。
- 主题专属性：三个自检和每日综合题分别要求非叶子语义、backbone 断图排查、RAW 可学习性 vs 价值证据，不是通用题干。

## 验收结果

- `check:curriculum --through=2026-09-09` 通过：09-02—09-09 共 24 门已备课、32 处 KaTeX 严格解析公式，最短正文 901 字符；同轨最高 5 字符 Jaccard 为 0.01421（09-08/09 路线课）。该值仅是重复筛查，不代表专家评分。
- `test:examples` 使用临时隔离环境 `/tmp/zhixing-course-20260904` 中的 PyTorch 2.7.1 实际运行：24/24 通过，0 失败。新增三例分别验证分支梯度/累加/VJP、detach 零梯度与 in-place 版本报错、RGB/RAW 路由覆盖；未下载权重或执行上游脚本。
- `test:daily` 通过：122 天/366 个唯一课程 ID，已备课课程计数 27（含 09-01 三门）；午夜/恢复、严格打卡、4 个迁移与旧表保留全部通过。测试只用内存 SQLite。
- `npx tsc --noEmit --incremental false`、`npm run lint`、`git diff --check` 均通过。
- `npm run build` 通过；Vinext 完成 client/server/RSC/SSR 五阶段构建，路由包含主页、课程详情与两个 API。

## 保护与未完成

- 未修改 09-01—09-08 已完成课程，未改变日期或任务 ID，未恢复通用正文兜底。
- 未连接 D1/R2、未发送打卡/作答/笔记/收藏/答疑写入，未运行机械臂、标定或客户数据上传。
- 09-10 及以后仍为已定总纲的待备课内容；本轮只宣称实写至 09-09。
