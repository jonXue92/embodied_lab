# 2026-09-09 课程备课与质量验收

## 新增范围

- 按 Asia/Shanghai 当天为 2026-09-09，原有已备正文至 09-12；为维持当日向后至少四天储备，新编并注册 09-13 三课。
- 基础课：推理权重、可续训 checkpoint、RNG 当前位置、数据采样位置与环境契约的状态边界。
- 代码课：model/optimizer/scheduler/RNG 的内存断点恢复、参数组顺序、LeRobot 两阶段恢复与故障注入。
- 路线课：代码/配置/数据/父 checkpoint 组成的实验身份卡、TensorBoard step/分支语义、日志与 checkpoint 成本预算。
- 三课时间 32+38+27=97 分钟，在 120 分钟日预算内保留 23 分钟复盘。

## 原始来源与实际阅读

- [PyTorch Saving and Loading Models](https://docs.pytorch.org/tutorials/beginner/saving_loading_models.html)、[Reproducibility](https://docs.pytorch.org/docs/stable/notes/randomness.html)：阅读 general checkpoint、随机源与可复现边界；区分 seed 起点与 RNG 当前状态，并保留跨版本/平台不保证逐位一致的限定。
- [Optimizer state_dict](https://docs.pytorch.org/docs/2.14/generated/torch.optim.Optimizer.state_dict.html)、[load_state_dict](https://docs.pytorch.org/docs/2.14/generated/torch.optim.Optimizer.load_state_dict.html) 与 [LRScheduler](https://docs.pytorch.org/docs/2.14/generated/torch.optim.lr_scheduler.LRScheduler.html)：核对 `state`/`param_groups`、参数 ID 的非业务主键性质、先构造 scheduler 再加载 optimizer 的顺序，以及 scheduler 自身相位。
- [PyTorch optimizer.py](https://github.com/pytorch/pytorch/blob/main/torch/optim/optimizer.py) 与 [serialization.py](https://github.com/pytorch/pytorch/blob/main/torch/serialization.py)：定位 `pack_group`/`param_mappings`、加载时的参数组长度检查和按序 zip，以及默认 zip 序列化与文件对象路径；main 核对日期 2026-09-09。
- [PyTorch TensorBoard](https://docs.pytorch.org/docs/2.14/tensorboard.html) 与 [writer.py](https://github.com/pytorch/pytorch/blob/main/torch/utils/tensorboard/writer.py)：定位 `SummaryWriter`、`add_scalar/global_step`、异步队列和 `flush/close`；没有把曲线误写成 checkpoint 或实验身份。
- [LeRobot lerobot_train.py](https://github.com/huggingface/lerobot/blob/main/src/lerobot/scripts/lerobot_train.py)：定位 `resume_before_prepare`、sampler offset、`resume_after_prepare` 与 `save_checkpoint`；核对 batch size/数据并行拓扑对精确样本恢复的影响。

## 人工内容复读

- 状态语义：公式 `S_{t+1}=F(S_t,B_{t+1};E)` 的 θ/o/q/r/d 均在正文定义；配置/数据 manifest 被写成解释语境，不混同可变训练张量。
- 恢复顺序：示例先构造 model/optimizer/scheduler，再加载 model、optimizer、scheduler，最后恢复 RNG；与官方 scheduler 警告一致。正例对齐 6 步连续轨迹和 3+3 恢复轨迹，负例只恢复权重与 RNG 并证明最终参数分叉。
- 边界声明：逐位检查只用于受控 CPU 回归；真实 GPU VLA 按结构、首步容差、短轨迹容差和多 seed 指标分层，不承诺跨平台 bitwise 等价。
- 实验身份：配置和数据使用 canonical JSON 指纹，父 checkpoint 与新分支分开记录；12 位短哈希只作人读 ID，正文要求完整 manifest 保留完整 SHA-256 与 schema。
- 成本算例：`T_redo,max=K·t_step` 与近似 checkpoint 时间占比定义了 K、步耗时、保存耗时；示例数字只用于个人算力决策，不外推为通用最优频率。
- 安全范围：三个脚本只运行 CPU/内存小对象，不落训练 checkpoint、不启用 TensorBoard 服务、不下载权重、不连接数据服务或机械臂。

## 验收结果

- `check:curriculum --through=2026-09-13` 通过：09-02—09-13 共 36 门已备课、49 处 KaTeX 严格解析公式，最短正文 901 字符；同轨最高 5 字符 Jaccard 为 0.02634（09-02/09-13 代码课）。该值只是重复筛查，不代表专家评分。
- `test:examples` 使用隔离环境 `/tmp/zhixing-course-20260904` 的 PyTorch 2.7.1 实际执行：36/36 通过，0 失败。新增三例分别验证 RNG 前沿恢复、完整续训与权重-only 负例、配置/数据指纹与分支守卫。
- `test:daily` 通过：122 个计划日、366 个计划课时，跨午夜、严格打卡与续学恢复通过；4 个迁移继续保留 `progress`、`answers`、`favorites`、`learning_answers`、`notes`、`qa_conversations` 六张用户数据表。
- TypeScript（禁用增量缓存）、ESLint、Vinext 五阶段生产构建与 `git diff --check` 均通过；构建包含首页、导出/状态 API 与课程详情路由。

## 情报复核

- 固定 24 小时窗口为 2026-09-08 23:31:51—2026-09-09 23:31:51（Asia/Shanghai），并桥接补查上轮截止后的 42 分 18 秒。16 个既定官方仓库没有窗口内相关新提交；上轮 Cosmos Framework `95f296fd` 已去重。
- 杭州/上海岗位搜索只得到旧岗位、聚合页抓取时间或缺少首发时间页面，可靠新增 0；题库式整理和旧面经均排除，可靠新面经 0。
- 无独立新信号簇，不满足总纲调整门槛；09-13 是既定主题的完整备课，不是情报驱动改纲。

## 保护边界

- 未修改 09-01—09-12 已完成课程，未改变日期或任务 ID，未恢复通用正文兜底。
- 未连接 D1/R2、未发送打卡/作答/笔记/收藏/答疑写入，未运行机械臂、标定或客户数据上传。
- 09-14 及以后仍为已定总纲的待备课内容；本轮只宣称实写至 09-13。

## 发布状态

- 复用现有 Sites 项目 `appgprj_6a8b0d47ffe48191ab5e4040761a1b85`，发布源码提交 `81e28ef0e87ba82884b9d319559d853e334b1b89` 为 v14；生产部署 `appgdep_6aa17fb4c4548191895593cbcfde43fa` 成功，地址保持为 <https://zhixing-embodied-lab.jonxue92.chatgpt.site>。
- 本地构建归档已通过 tar 清单与 SHA-256 校验，但向两个文件存储节点上传分别连接失败和超时；随后按 Sites 规范使用同一已推送提交的远程构建回退，未改变源码。
- 发布前后核验访问策略均为 `custom`、仅 owner、0 外部访客；未调整 D1 绑定，R2 仍为空，环境修订为 0。后台自动化未打开浏览器或前台预览。
