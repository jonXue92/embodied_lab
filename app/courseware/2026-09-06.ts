import type { AuthoredDay } from './types';
import { sources as s } from './sources';

export const day06: AuthoredDay = {
  reviewedAt: '2026-09-03', revision: '逐课编写 2.0',
  deliverable: 'SO-101 系统接口图、只读配置检查器，以及不含自动动作执行的采集安全清单。',
  question: { title: '主臂、从臂的数组都是六维，为什么仍不能不经校验直接互相复制？', summary: '从关节顺序、标定、单位、夹爪范围和发送后的物理反馈说明。', keywords: ['顺序', '标定', '单位', '夹爪', '反馈'], reference: '六维只说明长度一致，不说明每一维对应同一关节、同一方向或同一单位。主从臂需要匹配的关节名称、零位/方向/范围标定及归一化约定；夹爪可能与其他关节使用不同范围。应按语义映射并限制命令，再记录发送动作和测得状态，先低风险监督验证，不用 shape 相等代替硬件安全检查。' },
  lessons: {
    foundation: {
      title: 'LeRobot 与 SO-101：从六个数到一条真实采集链路', minutes: 30,
      prerequisites: '理解 observation/action、时间戳和归一化；不要求现在连接机械臂。',
      connection: '前几天讨论模型与动作队列，今天补齐模型外部的传感器、驱动、标定和数据记录。',
      intro: '模型输出六个浮点数，不代表从臂就知道该怎么动。硬件驱动、关节语义与校准把数值转成物理命令；数据记录还必须知道这条命令对应哪一幅图像。',
      outcomes: ['画出主臂、从臂、相机、数据集的职责边界', '解释编码器读数与归一化关节状态的区别', '区分建议动作、发送动作和观测到的执行结果'],
      sections: [
        { heading: '01 遥操作不是两个数组相等这么简单', body: '主臂由人操纵，读取其关节位置作为示教意图；从臂接收经过映射和限制的目标命令，实际完成动作；相机记录环境；采集器把状态、命令和图像关联成 episode。每个组件都可能延迟或失败，所以整条链需要明确接口。\n\n即使两边都有六个关节，也要逐项核对名称和顺序。数组第 3 项在一侧是肘关节、另一侧是腕部时，shape 完全正确却会产生错误动作。对于新手，第一张图应画“谁产生什么、谁消费什么”，不是直接安装模型开始推理。官方 SO-101 指南提供硬件流程，但具体接线和部件版本仍须现场核对。', sourceIds: ['so101'] },
        { heading: '02 标定是在建立数值与物理量之间的坐标关系', body: '电机原始编码器读数需要结合零位、方向和运动范围转换成有意义的关节状态。一个教学线性归一化可以把合法读数区间映射到 [-1,1]，但真实驱动可能使用角度或其他范围，夹爪也可能有独立定义。不要把本页公式当成 LeRobot 所有硬件版本的实现。\n\n标定错误的典型症状不是程序崩溃，而是“同一个数对应不同姿态”。更换电机、主从臂或归一化模式后，应重新核对映射。训练时使用的状态统计与部署驱动单位必须一致；否则即使 checkpoint 和图像不变，模型也会看到另一套关节坐标。', formulas: [{ latex: 'z=2\\frac{r-r_{\\min}}{r_{\\max}-r_{\\min}}-1,\\qquad r=r_{\\min}+\\frac{z+1}{2}(r_{\\max}-r_{\\min})', explanation: 'r 为教学用原始读数，z 为归一化值；r_max 必须大于 r_min。真实驱动的角度、零位和夹爪约定请查当前配置，不直接套此式。' }] },
        { heading: '03 图像与关节不是天然同时采到的', body: '相机曝光、图像解码、电机读取和动作发送发生在不同时间。即使采集器将它们写入同一行，也不代表物理同时。抓取高速物体时，几十毫秒差异就可能导致图像里的位置与状态记录不一致。你熟悉图像与 LiDAR 同步，这里可以沿用“采集时刻不是到达时刻”的判断。\n\n先记录各来源时间戳及其时钟域，再决定最近邻、插值或同步触发等策略。若相机和主机不是同一时钟，需要估计偏移与漂移。不要先将所有记录标成同一个 now，再期待神经网络学习补偿；那会隐藏延迟来源，也让之后的控制误差无法复盘。' },
        { heading: '04 一条动作链上存在三个不同事实', body: 'policy 或主臂提出的动作是 proposed，经过限位和速度限制后发出的命令是 sent，之后电机实际位置是 measured。它们不一定相同：安全层可能裁剪，执行器可能滞后，夹爪可能碰到物体。真实数据要保留这些差别，否则训练和评价会把安全系统的干预误当成模型能力。\n\n本次检查的 SOFollower.send_action 返回实际发送的目标，可作为日志读取点，但它不是机器人一定到达该目标的证明。最终执行事实仍需下一次观测。先把离线回放做通，再在你监督下按设备指导连接；课程本身不自动执行任何标定、遥操作或电机写入。', sourceIds: ['follower'] },
      ],
      points: ['长度相同不等于关节语义相同。', '标定与归一化定义数值到物理量的映射。', 'proposed、sent、measured 必须区分。'],
      codeTitle: 'normalization_roundtrip.py · 原创纯数值例子',
      code: ['def normalize(raw, low, high):', '    assert high > low', '    return 2 * (raw-low)/(high-low) - 1', '', 'def denormalize(z, low, high):', '    return low + (z+1)*(high-low)/2', '', 'for raw in [100, 500, 900]:', '    z = normalize(raw, 100, 900)', '    assert abs(denormalize(z, 100, 900)-raw) < 1e-9', 'print("roundtrip verified; no hardware accessed")'].join('\n'),
      codeNotes: ['往返一致只能证明数学实现自洽，不能证明 low/high 来自正确硬件标定。', '本脚本不导入硬件驱动，也不打开串口。'],
      exercise: { prompt: '假设正确零位为 500，却误把它设为 450，说明可能造成什么问题。', steps: ['区分归一化坐标偏移与物理传感器噪声。', '写出需要查看的标定记录与姿态对照证据。'], solution: '这是系统性坐标偏移，会让同一姿态对应不同数值；不能当成随机噪声靠平均消除。应核对校准文件、设备 ID、关节方向和已知参考姿态。' },
      quiz: { question: 'send_action 返回了目标值，为什么不能据此认定机器人已经到位？', hint: '区分命令与状态反馈。', keywords: ['发送', '反馈', '滞后', '状态', '测量'], reference: '返回值说明发送了什么，不保证实际执行到位。电机动态、接触和负载可能造成差异，应读取后续关节状态及必要外部观测，并保留命令与测量时间。' }, sources: [s.so101, s.follower],
    },
    code: {
      title: '读设备配置、标定与采集入口：先做只读契约检查', minutes: 35,
      prerequisites: '知道关节名称、单位、设备 ID 与相机时间戳的作用。',
      connection: '把基础课的接口图落实为可检查的配置字段；本节不重复训练或注意力代码。',
      intro: '硬件程序的第一步不是运行控制命令，而是确认你准备交给驱动的配置究竟指向什么。我们沿 LeRobot 的配置—驱动—采集路径阅读，并编写不访问串口的检查器。',
      outcomes: ['找到当前驱动的观测与动作特征定义', '区分读取状态与写入目标的函数', '用负例验证配置不兼容能被提前拒绝'],
      sections: [
        { heading: '01 源码阅读从特征契约开始', body: '在本次核对的 SOFollower 中，observation_features 和 action_features 描述驱动暴露的字段。get_observation 读取电机状态与相机数据，send_action 则处理目标命令并写入电机。先标记每个函数是否有外部副作用：阅读或构造配置不等于执行 connect/calibrate。\n\n这条区分很重要，因为某些连接流程会检查或更新标定。不要把运行示例当作无害的“看看输出”。本课要求打开源码定位函数，但只运行下面的纯 Python 字典检查器；不会调用官方驱动，也不会根据猜测的端口连接设备。', sourceIds: ['follower'] },
        { heading: '02 配置字段的消费者决定其意义', body: 'robot ID 用来关联设备与标定记录，port 指向通信接口，camera 名称定义视觉输入的键，关节单位影响数值解释。配置中存在一个字段不代表训练或采集真的使用了它，应追到构造器和实际消费者。比如相机顺序若只靠字典插入顺序隐式传递，就需要在模型输入处进一步确认。\n\n检查器应比较名称序列，而不是把名称变成集合后只检查相同。集合会忽略顺序，从而漏掉两个关节调换的危险情况。同时，单位应逐关节声明；夹爪的百分比或归一化范围可能不同于身体关节。这里采用教学配置，不宣称可以直接作为官方 CLI 参数。' },
        { heading: '03 从采集入口追到一帧数据的落盘', body: '查看当前 lerobot_record.py 时，先读模块说明与 record_loop，找到 observation 从哪里来、遥操作动作何时产生、最终写入哪些字段。本次 main 的说明明确将遥操作采集与策略 rollout 分离，因此不能照搬旧教程中把策略推理塞到采集命令里的用法。\n\n当源码与旧博客冲突，优先核对自己安装版本的帮助和对应仓库提交。记录函数名、版本和观察到的字段，比抄一行可能已经变化的 CLI 更耐用。对于数据上传步骤，应明确本地保存与公开发布的区别，客户 POC 的图像不因为脚本支持 push 就自动获得上传授权。', sourceIds: ['record'] },
        { heading: '04 用不兼容样本检验检查器', body: '下面构造模型契约和设备契约，比较关节顺序、单位和相机列表。正确配置通过，调换前两个关节后必须失败；这个负例证明检查器能发现语义错配，而不仅是永远返回 True。再增加缺少相机、重复关节名和标定 ID 不匹配的用例，可以逐步形成接入前测试。\n\n通过这类检查不等于可以无人值守运行机械臂。它只排除一部分静态错误，不能检测接线、实际限位、桌面障碍或电源异常。要在界面和文档中清楚区分“配置验证通过”和“硬件安全验收通过”，避免一个绿色勾号造成过度信任。' },
      ],
      points: ['先标出哪些函数读设备、哪些函数会写设备。', '配置兼容比较语义与顺序，不只比较维度。', '源码入口可能演化，记录版本而不盲信旧命令。'],
      codeTitle: 'device_contract.py · 原创只读字典验证',
      code: ['def validate(expected, actual):', '    for key in ("joints", "units", "cameras", "calibration_id"):', '        assert expected[key] == actual[key], f"mismatch: {key}"', '    assert len(set(actual["joints"])) == len(actual["joints"])', '', 'contract = {', '    "joints": ["shoulder", "elbow", "gripper"],', '    "units": ["degree", "degree", "percent"],', '    "cameras": ["wrist", "front"],', '    "calibration_id": "teaching-only",', '}', 'validate(contract, dict(contract))', 'bad = dict(contract, joints=["elbow", "shoulder", "gripper"])', 'try:', '    validate(contract, bad)', 'except AssertionError as error:', '    assert "joints" in str(error)', 'else:', '    raise AssertionError("joint-order mismatch missed")', 'print("contract check and negative case: PASS")'].join('\n'),
      codeNotes: ['只有三个教学关节，目的是演示语义检查，不是真实 SO-101 配置。', 'assert 适合教学测试；生产配置验证应使用显式异常，不依赖可被优化关闭的断言。'],
      exercise: { prompt: '让相机名保持相同但顺序颠倒，解释模型可能看到什么。', steps: ['运行检查器确认报错。', '说明何时字典按名称映射可以避免顺序依赖，何时 tensor 堆叠仍需要顺序。'], solution: '按键访问且后续显式指定 camera_names 可以避免隐式顺序；但堆成 [camera,...] 张量时仍需和训练一致，否则腕部图像可能被当作外部视角。' },
      quiz: { question: '为什么关节名集合相同、shape 相同的配置仍应可能被拒绝？', hint: '考虑顺序、单位和标定。', keywords: ['顺序', '单位', '标定', '语义'], reference: '集合忽略顺序，shape 不表达单位和标定。数值会被发送给错误关节或解释为错误幅度，因此应比较明确名称顺序、逐关节单位及设备标定标识。' }, sources: [s.follower, s.record, s.so101],
    },
    insight: {
      title: '真机实验的安全与可复现：从一段演示到可解释证据', minutes: 25,
      prerequisites: '理解驱动命令与测得状态的差别；知道配置检查不等于硬件验收。',
      connection: '今天把模型外部的风险与证据边界补齐，明天用第一周综合题检查整条链是否真正贯通。',
      intro: '一段成功视频可能来自十次失败中的一次，也可能使用了未记录的手工干预。可信的个人作品需要保留试验分母、失败原因和配置，更需要在每次测试前知道如何停下来。',
      outcomes: ['定义一次有效试验与成功标准', '说明少量试验成功率的不确定性', '列出从桌面检查到监督运行的分级验收'],
      sections: [
        { heading: '01 先定义成功，不在看见结果后修改标准', body: '“抓起来了”可能只维持一瞬，也可能移到目标区域后稳定放下。试验前要定义物体、初始范围、完成条件、超时和失败分类，并声明是否允许人工介入。否则两次方法比较可能实际上在不同任务上进行。\n\n例如把标准写成“在指定初始范围内抓取并放到目标区域，结束后稳定保持规定时间”，比“动作看起来流畅”更可复查。具体时间和范围要由任务与安全限制确定，本课不替你给机械臂设默认值。拍摄与日志应覆盖完整尝试，失败也进入分母，不只保存成功片段。' },
        { heading: '02 8/10 与 80/100 的证据强度不同', body: '两者点估计都是 80%，但后者通常具有更小的抽样不确定性。在独立同分布 Bernoulli 近似下，标准误差约为 sqrt(p(1−p)/n)：n=10 时约 0.126，n=100 时约 0.04。这个近似只帮助理解样本量，不宜拿十次试验直接套正态区间作强结论。\n\n真机试验还常违反独立假设，例如同一物体、同一光照连续运行。应报告具体场景和试验次序，按场景分组，必要时用 Wilson 区间或更合适的统计方法。最关键的是不把“少量成功”写成“泛化已证明”，把不确定性保留在作品说明里。', formulas: [{ latex: '\\hat p=s/n,\\qquad \\mathrm{SE}(\\hat p)\\approx\\sqrt{\\hat p(1-\\hat p)/n}', explanation: 's 为成功次数，n 为总有效试验数；SE 基于独立近似，不是安全置信保证。小样本或相关试验需更谨慎解释。' }] },
        { heading: '03 按风险逐级放行，而非一次跳到自主操作', body: '第一层离线：配置、单位、时间戳、数据回放与模型输出范围检查。第二层硬件准备：遵照设备说明核对装配、标定、工作空间与停止手段。第三层受监督的低风险测试：从有限动作范围和可控负载开始，并记录异常。每层都有明确通过条件，上一层失败就不进入下一层。\n\n这不是替代厂商安全手册，也不是允许无人监管运行。机械臂、夹爪和负载可能造成伤害或财物损失，现场判断由实际操作者负责。本课脚本只做离线计算，不执行端口探测、标定、电机配置或动作发送；教学中“自动测试”的范围必须与真机执行分开。', sourceIds: ['so101'] },
        { heading: '04 可复现材料应让别人知道你实际做过什么', body: '至少保存代码/模型版本、数据切分、标定标识、相机配置、任务协议、试验列表和失败复盘。如果存在人工协助，记录介入时刻与原因；如果只做了离线评测，明确没有验证真机成功率。你可以将私有原始数据留在本地，用去标识的统计和授权片段展示结果。\n\n对求职作品，完整说明一次失败如何被定位、如何修复、修复是否在固定任务上复测，通常比只展示一个高分更能体现工程能力。传感器知识的优势可以体现在时间对齐、光照失败分类和数据质量改进上，但仍需要让模型、数据和控制环节形成可追溯联系。' },
      ],
      points: ['先写成功标准和分母，再运行比较。', '成功率要同时报告样本量、场景和介入。', '自动离线检查不能冒充真机安全认证。'],
      codeTitle: 'success_uncertainty.py · 原创统计直觉算例',
      code: ['from math import sqrt', '', 'for successes, trials in [(8, 10), (80, 100)]:', '    p = successes / trials', '    se = sqrt(p * (1-p) / trials)', '    print(trials, round(p, 2), round(se, 3))', '# 10 0.8 0.126; 100 0.8 0.04 (iid approximation only)'].join('\n'),
      codeNotes: ['SE 不是“机器人出错概率”，而是点估计的抽样不确定性近似。', '真实试验可能相关，必须保留场景标签。'],
      exercise: { prompt: '为自己的演示视频补一张结果卡，不需要现在运行机械臂。', steps: ['写明总尝试次数、成功次数、人工介入与失败类型。', '标出尚未验证的场景或能力。'], solution: '合格卡片应能让读者分清已测与未测、成功与辅助成功；不应只用一个百分比替代协议和分母。' },
      quiz: { question: '10 次里成功 8 次，可以直接宣布对任意场景都有 80% 成功率吗？', hint: '考虑样本量、采样范围和独立性。', keywords: ['样本', '场景', '不确定', '分母', '泛化'], reference: '不能。80% 只是该试验协议下的点估计，样本小且场景有限；应报告次数、条件、介入和不确定性，不能外推到未测试环境。' }, sources: [s.so101, s.robotil],
    },
  },
};
