import type { AuthoredDay } from './types';
import { sources as s } from './sources';

export const day05: AuthoredDay = {
  reviewedAt: '2026-09-03', revision: '逐课编写 2.0',
  deliverable: '一个不跨 episode 的动作块切片器、同一目标时刻的融合测试、预测长度与执行长度的时延预算表。',
  question: { title: '50 Hz 控制、80 ms 推理、预测 20 步动作：应该每次执行 20 步再重新观察吗？', summary: '区分预测长度 K、执行长度 H、控制周期 Δt 和推理时延；给出能响应扰动的设计。', keywords: ['20', '80', '执行', '预测', '异步', '反馈'], reference: '50 Hz 对应 20 ms 周期，80 ms 推理跨 4 个控制 tick。K=20 只是预测 400 ms 的动作，不要求 H=20。可以执行较短前缀并异步计算新 chunk，但要检查观测到动作的总延迟、重叠时间索引、队列耗尽和过期预测。H 太短且没有并行/缓存会导致来不及供给动作，H 太长则闭环纠错慢；应以扰动恢复、p95 时延和安全回退共同选取。' },
  lessons: {
    foundation: {
      title: 'Action Chunk 的三个时间尺度：预测长度不等于闭环间隔', minutes: 30,
      prerequisites: '知道 [B,K,D] 动作块与 padding；理解策略动作会改变下一观测。',
      connection: '第一天认识 ACT，今天专门推导动作块的时间语义；和昨天的价值函数不同，重点是动作如何被执行。',
      intro: '一次生成多步动作可以表达连续运动，但机器人是否持续看见新观测，取决于执行策略而不只是模型输出 shape。先把预测窗口、执行窗口和控制周期分开，才能解释平滑与纠错的冲突。',
      outcomes: ['区分 K、H 和控制频率', '说明动作块为何能表示时序相关性', '计算最长开环段并识别反馈迟滞'],
      sections: [
        { heading: '01 从单步回归到联合轨迹预测', body: '单步策略给出当前动作，动作块策略给出未来 K 个动作。它的优势不只是少调用网络，还在于训练时能让多个动作共享上下文，表达一次抓取中的接近、闭合与抬起的相互关系。若数据存在多种合理轨迹，简单逐维均值可能产生两条轨迹之间不可行的中间动作，模型分布形式仍然重要。\n\nACT 将动作块作为核心建模对象，并使用潜变量刻画示教变化。但“有 K 维时间轴”本身不保证动作平滑，也不保证轨迹碰撞安全。必须区分表示能力、训练目标和执行策略，不能把 action chunk 当成自动解决所有控制问题的功能开关。', sourceIds: ['actpaper'], formulas: [{ latex: '\\hat A^{(t)}=[\\hat a_{t|t},\\hat a_{t+1|t},\\ldots,\\hat a_{t+K-1|t}]', explanation: '竖线右侧 t 是生成预测时刻，左侧是动作对应的目标执行时刻。同一个目标时刻可以存在来自不同起点的预测。' }] },
        { heading: '02 预测 K 步，可以只执行 H 步', body: '模型一次预测 K=20 步，并不要求全部执行完才再读取图像。如果每 5 步重新计算并替换后续动作，执行窗口 H=5；在 50 Hz 控制下，一个 tick 为 20 ms，20 步覆盖 400 ms，而 5 步仅覆盖 100 ms。滚动重规划用更近的新观测纠正未来动作。\n\n但更频繁重规划需要计算供给。若一次推理耗时 80 ms，同步地每步都等推理，就无法维持 50 Hz。你可以异步生产 chunk 或减小模型，也可以放宽执行窗口；每种方案都影响反应时间。仅看 K 无法判断闭环频率，必须同时记录 H、推理时延和相机更新率。', formulas: [{ latex: '\\Delta t=1/f_c,\\qquad T_{\\rm predict}=K\\Delta t,\\qquad T_{\\rm open}\\approx H\\Delta t', explanation: 'f_c 为控制频率；开环时间还可能叠加采集、传输和计算延迟，这里是忽略额外延迟的简化预算。' }] },
        { heading: '03 平滑性与新信息不是同一目标', body: '重规划时，新 chunk 与旧 chunk 可能给出不同的当前动作，直接切换会产生跳变。可以在同一目标时刻融合多个预测，也可在控制层限制变化率。但融合旧预测会保留旧观测带来的偏差；如果物体突然移动，过于平滑的输出可能反而更晚响应。\n\n想象夹爪接近时物体被挪走：旧预测继续朝原位置，新预测要求减速或撤离。把二者平均不一定是更安全的动作。安全事件下应由独立规则停止或回退，而不能让时间平均抵消紧急命令。因此平滑指标只是一个维度，还要检查扰动恢复和安全边界。' },
        { heading: '04 如何用小实验分离这些因素', body: '固定同一个 checkpoint、相机和任务，只改变 H 或融合权重，记录成功率、动作速度/加速度、p95 延迟和碰撞/接管事件。先离线回放核对目标时间索引，再在受控低风险任务中验证，不要一开始在快速高负载动作上调参数。\n\n如果 H 变小成功率提高，可能来自反馈更及时，而不一定是模型预测更准；如果曲线变平滑但成功率下降，检查是否融合了过旧的计划。每条 chunk 都保存生成时间、观测时间和覆盖区间，这样才能复盘“哪一刻的观测驱动了哪一刻的动作”，而不是只有一条难以解释的最终关节曲线。' },
      ],
      points: ['K 决定预测范围，H 决定一次执行多少步，两者必须分开。', '动作块表示连续计划，但不自动提供闭环反馈。', '平滑、低延迟和安全要分别测量。'],
      codeTitle: 'chunk_time_budget.py · 原创时间预算',
      code: ['hz, k, h, inference_ms = 50, 20, 5, 80', 'dt_ms = 1000 / hz', 'assert dt_ms == 20', 'assert k * dt_ms == 400', 'assert h * dt_ms == 100', 'print("ticks per inference:", inference_ms / dt_ms)  # 4.0', 'print("execution window ms:", h * dt_ms)'].join('\n'),
      codeNotes: ['实际系统还需计算相机、通信和队列延迟。', '这个预算只能排除明显不可能配置，不能代替时延测量。'],
      exercise: { prompt: '保持 K=20，把 H 改为 1 和 10，比较同步执行是否能按时获得下一组动作。', steps: ['分别算 20 ms 和 200 ms 的窗口。', '与 80 ms 推理时间比较。'], solution: 'H=1 时同步推理赶不上 20 ms；H=10 有计算余量，但至少 200 ms 才重新规划的设计可能反应更慢。异步方案需要进一步检查总延迟和队列剩余长度。' },
      quiz: { question: '为什么 K=20 不意味着控制一定是 2.5 Hz？', hint: '400 ms 预测范围不等于每 400 ms 才控制一次。', keywords: ['预测', '执行', 'H', '频率', '重规划'], reference: '模型可以以 50 Hz 消费动作，同时只执行 H<K 的前缀并重规划。2.5 Hz 只对应每 400 ms 产生一整块且执行完再更新的特定设计，不是 K 本身决定的控制频率。' }, sources: [s.actpaper, s.actcode],
    },
    code: {
      title: 'Chunk 切片、padding 与 temporal ensemble：先对齐目标时刻', minutes: 35,
      prerequisites: '会区分 chunk 生成时间和目标执行时间；能理解有效位。',
      connection: '9 月 2 日学习 mask loss；今天不重复训练循环，而是处理时序切片和在线融合的索引。',
      intro: '把几条“看起来同样长”的动作数组平均并不叫 temporal ensemble。首先要从每条预测中取出对应同一目标时刻的元素，再用明确权重融合，且不能把零动作误当成空位。',
      outcomes: ['实现 episode 边界内的动作补齐', '用显式有效位保留合法零动作', '为当前目标时刻融合跨预测起点的候选动作'],
      sections: [
        { heading: '01 切片要在 episode 内结束', body: '从起点 t 提取 K 个动作时，真实长度是 min(K,T−t)。剩余位置可以补零或重复末帧，但不管填什么都应标为无效；补齐方式不应通过 loss 偷偷变成监督信号。若对多条 episode 拼接后的数组直接切片，尾部可能接上下一次任务的动作，模型会学到物理上不连续的未来。\n\n下方例子用标量动作简化演示：episode=[0,2,4]、t=2、K=3，返回 [4,0,0] 和 [True,False,False]。起点 t=0 时第一个零却是合法动作。因此“值为零”与“没有样本”是两件事，不能靠 nonzero 或绝对值阈值推断有效性。', sourceIds: ['actdata'] },
        { heading: '02 融合前先查每条预测的覆盖区间', body: '设时刻 0 预测 [1,2,3]，时刻 1 预测 [10,20,30]。当前目标时刻 t=2 时，第一条应取索引 2，第二条应取索引 1，得到 3 和 20；不是把两条数组的最后一个元素 3 和 30 平均。每条预测都必须同时保存 start 与动作数组长度。\n\n过滤条件为 start≤t<start+K，过期或尚未覆盖的预测不能进入。若候选为空，应显式触发队列耗尽处理，而不是返回零向量冒充安全动作。零位置在某些机器人上可能意味着大幅运动，所以实际回退策略必须由硬件和任务安全设计决定。' },
        { heading: '03 权重的方向由排序决定', body: 'ACT 原始示例中，可以沿 all_time_actions 的时间轴看到候选排序，再检查 exp_weights 如何随索引变化。不能看到 exp(-k×index) 就凭直觉说“越新越重要”：如果 index 从最早预测开始递增，正 k 反而更偏旧预测。阅读代码必须把权重公式与数组顺序一起解释。\n\n本课选择另一种明确的教学定义：按候选年龄 age=t−start 指数衰减，使较新预测权重较大。这不是声称原仓库采用同样方向，而是一个独立可测试实现。用户可以比较两种策略，但必须在实验记录中写明权重定义，避免同名参数代表相反含义。', formulas: [{ latex: 'w_i=\\frac{e^{-\\lambda(t-s_i)}}{\\sum_j e^{-\\lambda(t-s_j)}},\\qquad \\bar a_t=\\sum_i w_i\\hat a_{t|s_i}', explanation: 's_i 是预测起点，λ≥0；本教学实现按年龄衰减。λ=0 为均匀平均，λ 增大更偏新预测。' }], sourceIds: ['actcode'] },
        { heading: '04 测试需要包含零动作与无候选', body: '除了正常两候选平均，还要测试合法零动作能进入融合、未来 chunk 被忽略、所有 chunk 过期时报错，以及目标时刻恰在区间右边界时不再有效。边界测试应先于真机连接，避免靠观察机械臂是否“看起来顺”来发现索引错误。\n\n下面脚本只处理一维标量，真实 [D] 动作可以逐维加权，但旋转和姿态表示并不总能直接欧氏平均。若动作包含旋转矩阵或四元数，需要额外的几何处理与归一化。不能因为关节角例子可平均，就把同一个函数原样套到所有控制空间。' },
      ],
      points: ['补齐有效性由 mask 表示，不从动作数值猜测。', '融合对象必须对应同一个目标时刻。', '权重方向要同时看公式和排序，不能凭名称判断。'],
      codeTitle: 'chunk_index_tests.py · 原创标准库实现',
      code: ['from math import exp', '', 'def window(episode, t, k):', '    assert 0 <= t < len(episode) and k > 0', '    part = episode[t:t+k]', '    return part + [0.0]*(k-len(part)), [True]*len(part)+[False]*(k-len(part))', '', 'def fuse(chunks, t, decay=0.1):', '    values, weights = [], []', '    for start, actions in chunks:', '        if start <= t < start + len(actions):', '            values.append(actions[t-start])', '            weights.append(exp(-decay*(t-start)))', '    if not values:', '        raise ValueError("No prediction covers target time")', '    return sum(v*w for v, w in zip(values, weights))/sum(weights)', '', 'assert window([0, 2, 4], 2, 3) == ([4, 0.0, 0.0], [True, False, False])', 'assert fuse([(0, [1, 2, 3]), (1, [10, 20, 30])], 2, 0) == 11.5', 'assert fuse([(0, [0.0]), (0, [2.0])], 0, 0) == 1.0', 'try:', '    fuse([(0, [1])], 1)', 'except ValueError:', '    pass', 'else:', '    raise AssertionError("expired chunk was accepted")', 'print("chunk boundaries and zero action: PASS")'].join('\n'),
      codeNotes: ['目标 t=2 的两个候选是 3 和 20，均匀平均为 11.5。', '真实控制需另行处理设备限位、关节单位和缺预测回退，脚本不会连接硬件。'],
      exercise: { prompt: '把均匀平均改为 decay=1，手算新旧候选权重大小关系。', steps: ['比较起点 0 与起点 1 在 t=2 的 age。', '验证输出应更接近 20，而不是 3。'], solution: '年龄分别 2 和 1，未归一化权重 e^-2 和 e^-1；新预测权重更大，融合结果约 15.43。这只说明权重行为，不代表控制性能一定改善。' },
      quiz: { question: '为什么用动作向量非零来判断预测是否存在会出错？', hint: '构造合法零关节命令即可反证。', keywords: ['零', '有效', 'mask', '时间', '存在'], reference: '零可能是合法目标或增量，不能表示缺失。应使用独立有效位或明确的覆盖时间区间；同时检查候选对应同一目标时刻，而不是只检查数组长度。' }, sources: [s.actdata, s.actcode],
    },
    insight: {
      title: '推理频率、平滑与纠错：如何选择动作队列的长度', minutes: 25,
      prerequisites: '理解 K 与 H 的区别，以及当前动作可以来自不同预测起点。',
      connection: '把动作块索引提升为系统选择：你的 Mac 推理与 SO-101 控制之间需要怎样的缓冲和回退。',
      intro: '动作队列不是越长越稳。它能吸收推理抖动，也会把旧观测的计划保留更久。本课用剩余动作数与最坏时延的关系，设计一个不依赖模型品牌的调参思路。',
      outcomes: ['估计队列何时耗尽', '解释平均时延不足以选执行窗口', '提出含突发扰动和排队抖动的评测'],
      sections: [
        { heading: '01 队列是计算供给与控制消费之间的缓冲', body: '控制器每个 tick 消费一个动作，推理器每次产出一段 chunk。如果剩余动作覆盖时间小于下一次推理完成时间，就可能断供。异步执行允许一边消费旧动作一边计算新动作，但不消除算力需求，只改变了等待发生的位置。\n\n设剩余 R=6 步、频率 50 Hz，队列覆盖 120 ms；若下一次推理加通信预计 90 ms，理论上还有 30 ms 余量。但若偶发延迟达到 150 ms，仍会耗尽。这里数字都是教学假设，真实阈值需测量并包括负载变化，不应直接写成你机械臂的安全配置。', formulas: [{ latex: 'T_{\\rm reserve}=R/f_c,\\qquad T_{\\rm reserve}>L_{\\rm inference}+L_{\\rm transfer}+L_{\\rm margin}', explanation: '这个不等式是供给预算条件，不是安全证明。L_margin 用于吸收抖动，应结合尾部时延和实际回退机制选择。' }] },
        { heading: '02 长队列缓解断供，却可能放大信息陈旧', body: '若物体位置改变，而队列还保存 400 ms 前的计划，动作流可以十分平滑却朝错误目标持续运动。解决方式包括缩短执行前缀、重叠更新、按时间戳丢弃旧动作以及异常触发停止。每种做法都会改变连续性和计算预算，不能只以“队列从不断”作为验收标准。\n\nSmolVLA 的公开工作展示了异步推理方向，值得学习生产者与消费者的分工。但在你的设备上，观测新鲜度、相机吞吐、主机负载和串口通信都不同。应复用思路与可检查接口，不照搬演示中的速度结论。', sourceIds: ['smolvla'] },
        { heading: '03 怎样设计能揭示差异的对照', body: '保持 checkpoint、数据和控制频率固定，只改变 H 或更新策略。静态摆放任务主要测试稳定执行，轻微可控位置变化测试纠错，人工注入推理延迟测试断供与回退。三类测试分别揭示不同失败，混成一个平均成功率会遮蔽原因。\n\n记录每个动作所依据观测的年龄，而不是只有模型调用耗时。例如推理 60 ms 很快，但相机帧已经排队 200 ms，控制仍依赖陈旧信息。把曝光/采集时间、推理开始、推理结束与发送时刻串起来，你的传感器时间同步经验会直接帮助解释系统表现。' },
        { heading: '04 安全回退不是一个零向量', body: '当预测超时、队列耗尽或观测失效时，系统应进入事先定义并经过验证的回退状态。位置控制下把命令设为全零可能让机器人移动到零位，并不等于停止；保持位置也可能在夹持或接触状态下带来风险。因此回退需结合机械结构、负载和制造商指导，不能由通用教学脚本替你决定。\n\n本周只做离线时间线和安全检查表，不自动下发动作。后续真机测试应由你在可控环境中监督，明确限位、工作空间和急停方式。是否继续使用更复杂的异步机制，以可靠性和可解释结果决定，而不是为了让架构图更复杂。' },
      ],
      points: ['队列长度是缓冲预算，也是观测陈旧风险。', '测尾部时延与观测年龄，不只测平均推理时间。', '超时回退必须按控制语义设计，零命令不等于安全。'],
      codeTitle: 'queue_budget.py · 原创离线预算检查',
      code: ['def reserve_ms(remaining, hz):', '    return 1000 * remaining / hz', '', 'reserve = reserve_ms(6, 50)', 'assert reserve == 120', 'assert reserve > 90', 'assert not reserve > 150', 'print("120ms reserve: normal latency fits; tail latency does not")'].join('\n'),
      codeNotes: ['脚本只检查时间预算，不判断某个机械臂是否安全。', '不要将示例数值直接用于设备的速度或限位配置。'],
      exercise: { prompt: '为你当前主机写出要测的四个时间戳，并说明每个差值回答什么。', steps: ['区分采集到输入的等待和网络计算。', '再计算发送时动作对应观测的年龄。'], solution: '采集时刻、推理开始、推理结束、动作发送；它们分别分离排队、推理、发送等待，并能计算发送时观测年龄。实际硬件不同钟源时还需要同步或偏移估计。' },
      quiz: { question: '平均推理 60 ms、p95 为 160 ms，队列覆盖 100 ms，为什么仍可能卡顿？', hint: '控制消费不会因为平均值好看而暂停。', keywords: ['尾部', '160', '100', '耗尽', '回退'], reference: '超过 100 ms 的推理会在下一批动作到达前耗尽队列；平均 60 ms 掩盖尾部。需要测量更完整的延迟分布，设计缓冲、提前触发和超时回退，并验证观测不过度陈旧。' }, sources: [s.smolvla, s.actcode, s.so101],
    },
  },
};
